[project]
name = "weather-skills"
version = "0.1.0"
description = "MCP Weather Server demonstrating Tools, Resources, and Prompts (Skills)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]
